
var config = {
    funcs: { //重载aspx表单上的计算、验证等函数
    },
    initialize: function (form) {
        //form.on({
        //    formload: function () { //表单数据加载完毕事件
        //        var me = this,
        //            postContainer = me.postContainer, //表单所在的提交和处理顶层容器
        //            viewmodel = me.viewmodel; //数据视图

        //        viewmodel.rootRow.tables['Purchase'].Rows[0].set('Reason', '9999'); //设置初始值
        //    },
        //    Submit: function (model, action, validationGroup, data) { //提交事件
        //        alert('aaa');
        //        return false; //return false阻止提交
        //    }
        //});
    }
};